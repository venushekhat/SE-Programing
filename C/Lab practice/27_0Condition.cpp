#include<stdio.h>
main()
{
	int number;
	 printf("\n\n\t Enter Number :");
	 scanf("%d",&number);
	 
	 if(number==0)
	 printf("\n\n\tThis number is zero");
	 
	 else
	 printf("\n\n\ttis number is not zero");
}
